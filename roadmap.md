# Roadmap: развитие проекта «Пикфлоуметр» (PeakFlow Bot)

> **Дата последнего обновления:** 2026-09-16 (документ исторический — описывает состояние на эту дату)
> **Актуальный статус:** см. `docs/superpowers/specs/2026-09-17-todo-plan.md` — Фазы 0–2, 4 (кроме снятой 4.3) и 5 выполнены, схема v5, **598 тестов**; Фаза 3 — по требованию.
> **Технические детали:** `PROJECT.md` и `wiki.md`.
> **Выполнено (2026-09-16):** Фазы 1–3 — критические баги, рефакторинг, инфраструктура/тесты/CI. См. раздел «✅ Выполнено» ниже.
> **Стратегия:** Превращение из семейного бота в публичный мульти-сервис для любых семей
> **Приоритеты:** 1) Критические баги, 2) Рефакторинг, 3) Инфраструктура и тесты, 4) Новые функции, 5) Развитие Mini App

---

## ✅ Выполнено 2026-09-16 (Фазы 1–3)

### Фаза 1 — критические баги

| # | Было | Стало |
|:-:|------|-------|
| 1.1 | Молчаливая инверсия утро↔вечер при занятом слоте | Бот показывает выбор «☀️ Утро / 🌙 Вечер» и пишет в выбранный слот (`cb_pick_tod`); статистика не искажается |
| 1.2 | `matplotlib`-рендер блокировал event loop | `_render_chart_png_async` через `asyncio.to_thread` (тест проверяет, что работа идёт в рабочем потоке) |
| 1.3 | `int(callback.data.replace(...))` без обработки | `parse_callback_int(data, prefix)` + alert во всех хендлерах (`h_`, `t_`, `edit_`, `del_`, `del_confirm_`, `hist_page_`) |
| 1.4 | FSM «залипал» на нецифровом вводе | Фидбек-подсказки по каждому FSM-состоянию (`_FSM_HINTS`) + команда `/cancel` |

### Фаза 2 — рефакторинг

- ✅ Весь SQL в `database.py`: добавлена `get_measurement_by_id`; `_save_edit_any`, `cb_edit_any`, `cb_delete*` больше не открывают `sqlite3.connect` напрямую.
- ✅ Удалён мёртвый код: `safe_delete`, `_show_history_from_message`, `Measurement.waiting_delete_confirm`, `PEF_NORM_BY_AGE`, `get_week_measurements`; убраны неиспользуемые импорты (`sqlite3`, `MONTH_NAMES`, `get_setting`, `get_measurements_for_chart`).
- ✅ Единый источник TZ: `database.py` импортирует `TZ_OFFSET` из `config`.
- ✅ Единая `get_effective_target(db_path, fallback)` в `database.py`; дубли из `config.py`/`bot.py`/`web/api.py` удалены.
- ✅ Markdown экранируется (`report.escape_md`) для `CHILD_NAME` и имён в уведомлениях.
- ✅ CSV через модуль `csv` (запятые/кавычки в именах и заметках больше не ломают файл).
- ✅ **Побочный фикс:** заметка после замера теперь привязывается к реально перезаписанной auto-записи (раньше могла уйти на другую, более новую запись).

### Фаза 3 — инфраструктура и тесты

- ✅ GitHub Actions CI (`.github/workflows/ci.yml`): pyflakes + compileall + pytest на Python 3.11/3.12.
- ✅ Версионированные миграции: `PRAGMA user_version` + `SCHEMA_VERSION` в `init_db`.
- ✅ Тесты запускаются **без `.env`**: `test/conftest.py` задаёт dummy `BOT_TOKEN` (закрывает исторический пункт №27b).
- ✅ `requirements-dev.txt` дополнен `pyflakes`.
- ✅ Документация (`wiki.md`, `PROJECT.md`, `README.md`) синхронизирована с кодом.
- ✅ Тестов: **197** (было 173).

> Фазы 4–5 (публичный мульти-сервис: мульти-тенант, PostgreSQL, REST API, биллинг, PDF, геймификация, развитие Mini App) — в работе, см. ниже.

---

## 📋 Комплексный план развития проекта (2026-09-16)

### Видение проекта

**Цель:** Трансформация из семейного бота (1 ребёнок + 2 родителя) в публичный сервис для любых семей с поддержкой мультидетных семей, регистрацией, подпиской и масштабируемой архитектурой.

**Целевая архитектура:**
- Мульти-тенантная система (несколько семей на одном инстансе)
- Telegram Web App как основной интерфейс (бот — дополнительный канал уведомлений)
- REST API для будущих мобильных приложений
- Микросервисная архитектура (бот, веб, API, уведомления, отчёты)
- PostgreSQL вместо SQLite для продакшена
- Docker-контейнеризация и оркестрация

---

## 🔴 Фаза 1: Критические баги (неделя 1)

### 1.1 Молчаливая инверсия утро↔вечер при повторном замере
**Где:** `bot.py:369-370` (`_save_measurement`)  
**Суть:** если оба слота дня заняты, авто-время суток инвертируется без уведомления  
**Влияние:** искажение статистики утро/вечер, некорректные недельные сравнения  
**Фикс:**
- [x] Явный вопрос пользователю при занятом слоте: «Утро или вечер?»
- [ ] Разрешение нескольких замеров одного времени суток (счётчик вместо булевой логики)
- [x] Тест на все комбинации: утро/вечер/оба/повторные

### 1.2 Блокирующие вызовы в event loop
**Где:** все SQLite-операции и `matplotlib` в хендлерах  
**Суть:** синхронные вызовы блокируют async event loop  
**Влияние:** бот «замирает» при нагрузке  
**Фикс:**
- [ ] `asyncio.to_thread()` для всех SQLite-операций (пока только рендер графика)
- [x] `asyncio.to_thread()` для генерации графиков
- [ ] Долгоживущее соединение SQLite (connection pool)
- [ ] Тест производительности: 100 одновременных запросов

### 1.3 Небезопасный парсинг callback_data
**Где:** `bot.py:547, 587, 616, 638, 476, 489`  
**Суть:** `int(callback.data.replace(...))` без try/except  
**Влияние:** неожиданный payload роняет хендлер  
**Фикс:**
- [x] Хелпер `parse_callback_int(data, prefix)` с fallback
- [x] Валидация всех callback_data перед использованием
- [x] Тесты на некорректные payload

### 1.4 FSM «залипает» при нецифровом вводе
**Где:** `bot.py:1091-1096` (`catch_all`), `bot.py:714`  
**Суть:** текст во время ввода ПСВ или цели игнорируется  
**Влияние:** пользователь не понимает, что делать  
**Фикс:**
- [x] Хендлеры нецифрового ввода для всех FSM-состояний
- [x] Команда `/cancel` для выхода из FSM
- [ ] Кнопка «Отмена» в клавиатурах ввода (пока только `/cancel` и «⬅️ Назад»)
- [x] Тесты FSM: валидный/невалидный ввод, отмена

---

## 🟠 Фаза 2: Рефакторинг и чистка кода (неделя 2)

### 2.1 Весь SQL через `database.py`
**Где:** `bot.py:451-456, 589-592, 617-621, 639-642`  
**Суть:** прямой SQL в `bot.py` в обход `database.py`  
**Фикс:**
- [x] Функции `get_measurement_by_id()`, `update_measurement_as_parent()`, `delete_measurement_as_parent()` в `database.py`
- [x] Замена прямого SQL в `bot.py` на вызовы функций
- [x] Удаление мёртвых функций: `get_week_measurements()`, `safe_delete()`
- [x] Тесты на новые функции

### 2.2 Удаление мёртвого кода
**Что удалить:**
- [x] `safe_delete` (`bot.py:156`)
- [x] `_show_history_from_message` (`bot.py:817`)
- [x] `Measurement.waiting_delete_confirm` (`bot.py:98`)
- [x] `PEF_NORM_BY_AGE` (`config.py:33`)
- [x] `ZONE_RED` (`config.py:44`) — документировать порог 60%
- [x] `get_week_measurements` (`database.py:256`)
- [x] Хендлер `cb_stats` — доступен ребёнку через кнопку «📈 Моя статистика»

### 2.3 HTML вместо Markdown
**Где:** все `parse_mode="Markdown"`  
**Суть:** `_` и `*` в `CHILD_NAME` ломают рендер  
**Фикс:**
- [ ] `DefaultBotProperties(parse_mode=ParseMode.HTML)` (пока legacy-Markdown + `escape_md`)
- [x] `html.escape()` для всех пользовательских данных
- [x] Тесты на специальные символы в имени

### 2.4 CSV через модуль `csv`
**Где:** `bot.py:774-787`, `report.py`  
**Суть:** ручная склейка строк без экранирования  
**Фикс:**
- [x] `csv.writer` с `QUOTE_ALL` или `QUOTE_MINIMAL`
- [x] UTF-8 BOM сохранить (`utf-8-sig`)
- [x] Тесты на запятые/кавычки в заметках

### 2.5 Единый источник TZ и целевой ПСВ
**Где:** `config.py:11`, `database.py:7`, `bot.py:105`  
**Фикс:**
- [x] Импорт `TZ` из `config` в `database`
- [x] Одна функция `get_effective_target()` (с БД)
- [x] Удаление дублирующей версии из `bot.py`

---

## 🟡 Фаза 3: Инфраструктура и тесты (недели 3-4)

### 3.1 GitHub Actions CI
**Файл:** `.github/workflows/ci.yml`  
**Задачи:**
- [x] Запуск тестов на push/PR
- [x] Проверка стиля кода (black, flake8)
- [ ] Сборка Docker-образа (будущая)
- [ ] Деплой на тестовый сервер (будущее)

### 3.2 Версионированные миграции БД
**Суть:** нет версионирования схемы, дрейф между кодом и БД  
**Фикс:**
- [x] `PRAGMA user_version` для версии схемы
- [ ] Функция `migrate_db(db_path, from_version, to_version)`
- [ ] Миграции: 0→1 (legacy-таблицы), 1→2 (индексы), 2→3 (source/note)
- [x] Тесты на миграции с пустой/старой/прод-БД

### 3.3 Тесты хендлеров и FSM
**Суть:** тесты импортируют `bot.py` целиком, нужны валидный env/aiogram  
**Фикс:**
- [x] Mock-объекты aiogram для хендлеров
- [x] Фикстуры для FSM-состояний
- [ ] Покрытие: все callback + текстовые хендлеры
- [x] Регрессионные тесты на баги Фазы 1

### 3.4 Актуализация документации
**Файлы:** `README.md`, `PROJECT.md`, `wiki.md`  
**Задачи:**
- [x] Синхронизация с текущим поведением (173 теста, Mini App)
- [x] Удаление устаревших разделов (50–800, 19 тестов, 🌅/🌆)
- [x] Документирование новых функций (заметки, auto-carry, CSV по периодам)
- [ ] Архитектурная диаграмма (бот + web + API)

### 3.5 Покрытие тестов
**Цель:** 90%+ покрытия (сейчас ~70%)  
**Задачи:**
- [ ] `codecov` интеграция
- [x] Тесты на блокирующий код (asyncio.to_thread)
- [ ] Интеграционные тесты (бот + БД + web)
- [ ] Нагрузочные тесты (100+ одновременных пользователей)

---

## 🔵 Фаза 4: Новые функции — публичный сервис (месяцы 2-4)

### 4.1 Мульти-тенантная архитектура
**Spec:** `docs/superpowers/specs/2026-10-01-multi-tenant-design.md`  
**Задачи:**
- [ ] Таблица `families` (id, название, настройки, подписка)
- [ ] Таблица `users` (id, family_id, role, telegram_id, имя)
- [ ] Таблица `children` (id, family_id, имя, дата рождения, целевая ПСВ)
- [ ] Миграция данных из текущей схемы
- [ ] API: регистрация семьи, приглашение участников
- [ ] Бот: команда `/link` для привязки к семье

### 4.2 PostgreSQL для продакшена
**Суть:** SQLite не масштабируется на тысячи семей  
**Задачи:**
- [ ] `database.py` абстракция: SQLite для тестов, PostgreSQL для прода
- [ ] Миграции схемы на PostgreSQL (Alembic)
- [ ] Connection pool (asyncpg)
- [ ] Тесты на PostgreSQL (Docker Compose)

### 4.3 Docker-контейнеризация
**Файлы:** `Dockerfile`, `docker-compose.yml`  
**Задачи:**
- [ ] Мульти-стейдж сборка (venv → runtime)
- [ ] Docker Compose: бот + web + PostgreSQL + Caddy
- [ ] Health checks для всех сервисов
- [ ] Тома для БД и бэкапов
- [ ] `.env` management через Docker secrets

### 4.4 Публичный веб-интерфейс
**Суть:** Telegram Mini App только для авторизованных через Telegram  
**Задачи:**
- [ ] Лендинг с регистрацией (email + пароль)
- [ ] Личный кабинет семьи (настройки, участники, дети)
- [ ] Дашборд с графиками (Chart.js или D3.js)
- [ ] Экспорт PDF/CSV по периодам
- [ ] Мобильная адаптация (responsive design)

### 4.5 REST API для мобильных приложений
**Spec:** `docs/superpowers/specs/2026-11-01-rest-api-design.md`  
**Задачи:**
- [ ] OpenAPI спецификация (Swagger)
- [ ] JWT аутентификация (access + refresh токены)
- [ ] CRUD: замеры, дети, настройки семьи
- [ ] Rate limiting (throttling)
- [ ] Версионирование API (`/api/v1/...`)

### 4.6 Подписка и биллинг
**Суть:** монетизация сервиса (freemium или подписка)  
**Задачи:**
- [ ] Тарифы: Free (1 ребёнок), Premium (мультидет, PDF, API)
- [ ] Интеграция с платёжной системой (ЮKassa, Stripe)
- [ ] Webhooks для оплаты/отмены
- [ ] Лимиты: замеры/месяц, экспорт, API requests

### 4.7 Геймификация (Subproject B)
**Spec:** `docs/superpowers/specs/2026-12-01-subproject-b-gamification-design.md`  
**Задачи:**
- [ ] Streak-счётчик (дни подряд с замерами)
- [ ] Достижения: «7 дней подряд», «30 дней», «100 замеров»
- [ ] Визуальные награды (бейджи в профиле)
- [ ] Уведомления о достижениях
- [ ] Тесты: начисление streak, edge cases (пропуск дня)

### 4.8 PDF-отчёты врачу (Subproject C)
**Spec:** `docs/superpowers/specs/2026-12-15-subproject-c-pdf-reports-design.md`  
**Задачи:**
- [ ] Генерация PDF (ReportLab или WeasyPrint)
- [ ] Шаблон отчёта: график + таблица + статистика
- [ ] Периоды: неделя/месяц/квартал/год
- [ ] Экспорт из бота и Mini App
- [ ] Печать: A4, ч/б совместимость

---

## 🟢 Фаза 5: Развитие Mini App (месяцы 5-6)

### 5.1 Улучшение графика
**Файл:** `web/static/app.js`  
**Задачи:**
- [ ] Zoom/pan (щипок на мобильных)
- [ ] Тултип при наведении (значение + дата + заметка)
- [ ] Переключение видов: линия/столбцы/точечный
- [ ] Сравнение периодов (эта неделя vs прошлая)
- [ ] Аннотации: события («болел», «лекарства»)

### 5.2 Push-уведомления
**Суть:** Telegram не всегда доставляет уведомления  
**Задачи:**
- [ ] Web Push API (браузерные push)
- [ ] Service Worker для офлайн-режима
- [ ] Настройки: время, частота, типы уведомлений
- [ ] Fallback на email/SMS (платно)

### 5.3 Мобильная адаптация
**Задачи:**
- [ ] Responsive дизайн (mobile-first)
- [ ] PWA (установка на домашний экран)
- [ ] Offline-режим (IndexedDB для кэша)
- [ ] Тесты на разных устройствах (BrowserStack)

### 5.4 Новые графики
**Задачи:**
- [ ] Распределение по зонам (pie chart)
- [ ] Heatmap недели (время суток × дни)
- [ ] Корреляция с заметками («болел» → падение ПСВ)
- [ ] Прогноз тренда (линейная регрессия)

### 5.5 Совместный доступ
**Задачи:**
- [ ] Приглашение врача (read-only доступ)
- [ ] Экспорт ссылкой (публичный график на 7 дней)
- [ ] Комментарии врача к замерам
- [ ] Уведомления врачу (опционально)

---

## 📊 Метрики успеха

| Метрика | Сейчас | Цель (3 мес) | Цель (6 мес) |
|---------|--------|--------------|--------------|
| Тесты | 197 | 220+ | 260+ |
| Покрытие | ~70% | 85% | 90%+ |
| Время ответа API | ~200ms | <100ms | <50ms |
| Одновременных пользователей | 3 | 100 | 1000+ |
| Семей в сервисе | 1 | 10 | 100+ |
| MRR (месячная выручка) | $0 | $50 | $500+ |

---

## 🗓️ Вехи (Milestones)

| Веха | Дата | Критерии готовности |
|------|------|---------------------|
| **M1: Стабильная версия** | 2026-09-30 | Фазы 1-2 завершены, все тесты зелёные |
| **M2: Инфраструктура** | 2026-10-31 | CI/CD, Docker, миграции БД, документация |
| **M3: Мульти-сервис** | 2026-11-30 | Регистрация семей, PostgreSQL, API v1 |
| **M4: Монетизация** | 2026-12-31 | Подписка, биллинг, PDF-отчёты |
| **M5: Публичный запуск** | 2027-01-31 | Лендинг, 10+ тестовых семей, поддержка |
| **M6: Масштабирование** | 2027-03-31 | 100+ семей, мониторинг, оптимизация |

---

## 📝 История аудита (архив)

> **2026-09-12:** Полный аудит проекта, исправление 5 критических багов + 7 позиций инфраструктуры. Все 173 теста проходят.

> **2026-09-14:** Mini App полностью паритетен боту (SP2a + SP2b + SP2c): чтение/запись/настройки/CSV/бэкап.

> **2026-09-16:** Стратегический план трансформации в публичный сервис (5 фаз, 6 месяцев).

---

*Ниже — исторический текст аудита от 2026-09-12 (сохранён для контекста). Исторические пункты, отмеченные как исправленные, уже неактуальны.*

---

## 📜 Исторический аудит (2026-09-12)

### ✅ Исправлено (Фаза 0 + быстрые победы, 2026-09-12)

| # | Что | Как |
|:-:|-----|-----|
| 1 | «✏️ Исправить последний» для родителей | `_save_edit_last` теперь передаёт `CHILD_ID` в `edit_measurement` + регрессионные тесты |
| 2 | `mark_reminder_sent()` стирал флаги дня | `INSERT ... ON CONFLICT(date) DO NOTHING` + `UPDATE` флага; регрессионный тест |
| 3 | Планировщик пропускал минуту | Новая `is_reminder_minute()` (окно 0–1 мин); тест окна |
| 4 | Двойной `callback.answer()` | `answer_callback` глушит `TelegramBadRequest`; убраны дубли в 9 хендлерах |
| 5 | `del_confirm_` без прав | Добавлена проверка `is_parent` в `cb_delete_confirm`; регрессионный тест |
| 11 | Нет индексов | `CREATE INDEX IF NOT EXISTS idx_meas_user_time`; тест наличия индекса |
| 15b | Нет кнопки «Назад» на шаге десятков | 3-й ряд в `kb_pef_tens`; тест клавиатуры |
| 18 | Префикс боевого токена в README/PROJECT | Заменён на плейсхолдер `123456789:AAE...` |
| 19 | Зависимости без пинов, pytest не указан | `requirements.txt` + `requirements-dev.txt` с пинами |
| 20 | Нет LICENSE | Добавлен `LICENSE` (MIT) |
| 24 | Мусор `test_peakflow.db-journal` | Фикстура чистит все суффиксы, файл удалён |

**Итог:** 33 теста (было 24), все зелёные. Устранено 5 критических багов + 7 позиций инфраструктуры.

### ✅ Реализовано в Подпроекте A (2026-09-12)

- ✅ Заметки к замеру (колонка `note`, вопрос после сохранения)
- ✅ Автозаполнение пропусков (auto-carry, `source='auto'`)
- ✅ Напоминания ребёнку (08:00/20:00) + эскалация родителям
- ✅ Настраиваемые часы напоминаний (⚙️ → «⏰ Напоминания»)
- ✅ Меню ребёнка: «📊 Мой график» + «📈 Моя статистика»
- ✅ График с навигацией по месяцам + PNG-экспорт
- ✅ CSV по периодам (всё/месяц, колонки «Заметка» и «Источник»)
- ✅ Кнопка «💾 Скачать бэкап» (только родители)

### 🔴 Осталось из исторического аудита (актуально для Фазы 1-2)

- 🔴 №6 — молчаливая инверсия утро↔вечер при повторном замере
- 🔴 №7 — блокирующие вызовы SQLite/matplotlib в event loop
- 🔴 №8 — небезопасный парсинг `callback_data`
- 🔴 №9 — FSM «залипает»: текст во время ввода ПСВ молча игнорируется; нет `/cancel`
- 🔴 №10 — дрейф схемы БД, нет версионированных миграций
- 🔴 №12–17 — прямой SQL в `bot.py`, дубль `get_effective_target`, TZ из двух мест, мёртвый код, Markdown без экранирования, CSV без `csv`-модуля
- 🔵 №21–23 — CI, git-конфиг, systemd + бэкапы (частично реализовано через `manage.sh`)
- 📚 №25–27b — устаревшая документация, тесты импортируют `bot.py` целиком

---

## 💡 Быстрые победы (польза/усилие) — историческая справка

| # | Действие | Время | Статус |
|:-:|----------|:-----:|:------:|
| 1 | Кнопка «Назад» на шаге десятков | ~15 мин | ✅ |
| 2 | LICENSE-файл | ~5 мин | ✅ |
| 3 | Плейсхолдеры токена в README | ~10 мин | ✅ |
| 4 | Пины зависимостей + requirements-dev | ~15 мин | ✅ |
| 5 | try/except в answer_callback | ~10 мин | ✅ |
| 6 | Регрессионный тест на edit_last | ~30 мин | ✅ |
| 7 | Индекс в БД | ~10 мин | ✅ |

---

## ✅ Что уже хорошо (актуально)

- WAL-режим SQLite; параметризованные SQL-запросы — инъекций нет
- Singleton-lock через `flock` — защита от двойного запуска
- Часовой пояс учтён везде (не только в планировщике)
- Проверка ID на `/start` — посторонние не пользуются ботом
- Валидация диапазонов (цель 100–800; ввод ПСВ 100–690)
- Пошаговый inline-ввод без набора текста — удобно ребёнку
- Дисклеймер «не медицинское устройство» во всех доках
- Секреты исключены из репозитория; `.env.example`, `.gitignore`, `wiki.md` на месте
- **197 тестов проходит** — отличное покрытие
- **Mini App полностью функционален** (чтение/запись/настройки/CSV/бэкап)
- `manage.sh` — установка, обновление, бэкапы, HTTPS через Caddy
- `report.py` — общие чистые хелперы для бота и Mini App

---

> **Приоритет выполнения:** Фаза 1 (критические баги) → Фаза 2 (рефакторинг) → Фаза 3 (инфраструктура) → Фаза 4 (публичный сервис) → Фаза 5 (Mini App). См. метрики успеха и вехи выше.

---

## ✅ Исправлено (Фаза 0 + быстрые победы, 2026-09-12)

| # | Что | Как |
|:-:|-----|-----|
| 1 | «✏️ Исправить последний» для родителей | `_save_edit_last` теперь передаёт `CHILD_ID` в `edit_measurement` + регрессионные тесты (unit + mock-хендлер) |
| 2 | `mark_reminder_sent()` стирал флаги дня | `INSERT ... ON CONFLICT(date) DO NOTHING` + `UPDATE` флага; регрессионный тест на все 3 типа |
| 3 | Планировщик пропускал минуту | Новая `is_reminder_minute()` (окно 0–1 мин) для утра/вечера/недельного; тест окна |
| 4 | Двойной `callback.answer()` | `answer_callback` глушит `TelegramBadRequest`; убраны дублирующие `answer()` в 9 хендлерах (add, add_force, history, hist_page, settings, export, chart, summary, stats); тест устойчивости |
| 5 | `del_confirm_` без прав | Добавлена проверка `is_parent` в `cb_delete_confirm`; регрессионный тест с CHILD_ID |
| 11 | Нет индексов | `CREATE INDEX IF NOT EXISTS idx_meas_user_time ON measurements(user_id, measured_at)` в `init_db`; тест наличия индекса |
| 15b | Нет кнопки «Назад» на шаге десятков | 3-й ряд в `kb_pef_tens` (хендлер back уже был); тест клавиатуры |
| 18 | Префикс боевого токена в README/PROJECT | Заменён на плейсхолдер `123456789:AAE...` во всех 4 местах |
| 19 | Зависимости без пинов, pytest не указан | `requirements.txt` (`aiogram==3.31.0`, `matplotlib==3.11.2`, `python-dotenv==1.2.3`) + `requirements-dev.txt` (`pytest==9.1.1`) |
| 20 | Нет LICENSE | Добавлен `LICENSE` (MIT, © itdevlog) — бейджи readme теперь честные |
| 24 | Мусор `test_peakflow.db-journal` | Фикстура чистит все суффиксы (`-wal`, `-shm`, `-journal`), файл удалён |
| — | Ложное ожидание в `test_csv_export_format` | Тест ожидал `260` в последней строке, хотя фактически там `250` (замеры добавляются с одинаковой минутой — порядок по id). Исправлен на корректное значение |

**Итог:** 33 теста (было 24), все зелёные. Устранено 5 критических багов + 7 позиций инфраструктуры.

**Осталось из аудита (актуальный список):**

- 🔴 №6 — молчаливая инверсия утро↔вечер при повторном замере (искажает статистику)
- 🔴 №7 — блокирующие вызовы SQLite/matplotlib в event loop
- 🟠 №8 — небезопасный парсинг `callback_data` (`int()` без try/except)
- 🟠 №9 — FSM «залипает»: текст во время ввода ПСВ молча игнорируется; нет `/cancel`
- 🟠 №10 — дрейф схемы БД (legacy `users`/`note`), нет версионированных миграций
- 🟠 №11b — LIKE-запросы по дате (индекс добавлен, но LIKE не использует его)
- 🟡 №12–17 — прямой SQL в `bot.py`, дубль `get_effective_target`, TZ из двух мест, мёртвый код (`safe_delete`, `stats`-хендлер без кнопки, `ZONE_RED`, `PEF_NORM_BY_AGE`), Markdown без экранирования, CSV без `csv`-модуля
- 🔵 №21 — CI (GitHub Actions); №22 — git-конфиг авторства; №23 — systemd + бэкапы БД
- 📚 №25–26 — PROJECT.md/README описывают устаревший ввод ПСВ («50–800», «19 тестов», эмодзи 🌅/🌆)
- 📚 №27b — тесты всё ещё импортируют `bot.py` целиком (нужны валидный env/aiogram); хендлеры без полного покрытия

---

*Ниже — полный текст аудита от 2026-09-12 (историческая справка; пункты, отмеченные выше как исправленные, уже актуальны).*

---

## 📌 Что изменилось с прошлого аудита (2026-09-03)

**Сделано:**
- ✅ Создан `.gitignore` (`.env`, `*.db*`, `.bot.lock`, `venv/`, `__pycache__/`).
- ✅ Создан `.env.example` без секретов.
- ✅ Написан `wiki.md` — полная документация логики бота по фактическому коду.
- ✅ Проект опубликован на GitHub (private, ветка `main`).

**Не исправлено:** критические баги №1–5 и всё остальное из прошлого аудита — на месте (проверено по коду).

**Факты прод-БД (проверено):** 183 замера, 114 строк в `reminders_sent`, 3 строки в legacy-таблице `users`, 1 в `parent_child_links`; **ни одного пользовательского индекса**; в `measurements` нет CHECK на `time_of_day`, но есть неиспользуемая колонка `note` и FK на `users(user_id)` — схема создана старой версией кода.

---

## 🔴 Критические баги (исправить в первую очередь)

### 1. «✏️ Исправить последний» не работает для родителей
- **Где:** `bot.py:430` → `edit_measurement(DB_PATH, mid, new_val, callback.from_user.id)`
- **Суть:** `edit_measurement()` (`database.py:101`) обновляет запись только при совпадении `user_id` записи (= `CHILD_ID`) с переданным ID. Родитель передаёт свой ID → `rowcount = 0` → всегда «❌ Не удалось изменить запись». Для ребёнка работает.
- **Фикс:** передавать `CHILD_ID` (как уже сделано в `_save_edit_any`, `bot.py:452`), либо добавить функцию родительского редактирования в `database.py`. Покрыть регрессионным тестом (см. №27).

### 2. `mark_reminder_sent()` стирает другие флаги дня
- **Где:** `database.py:284-287`
- **Суть:** `INSERT OR REPLACE INTO reminders_sent (date) VALUES (?)` пересоздаёт всю строку, сбрасывая соседние флаги в 0. Пример: утренний флаг поставлен в 10:00, вечером недельный отчёт делает REPLACE — флаг утра теряется. В прод-БД 114 дней истории — искажения уже возможны.
- **Фикс:** `INSERT ... ON CONFLICT(date) DO NOTHING` + отдельный `UPDATE ... SET <type> = 1`.

### 3. Планировщик может пропустить напоминание на весь день
- **Где:** `bot.py:1126, 1143, 1159`
- **Суть:** условие `hour == DEADLINE and minute == 0` при `sleep(60)`: если тик пришёл в 09:59:59, следующий будет ~10:01:00 — окно закрыто, напоминание молча не уйдёт.
- **Фикс:** проверять `minute < 2`, либо хранить время последнего тика и ловить «пересечение границы часа», либо APScheduler.

### 4. Двойной `callback.answer()` → риск TelegramBadRequest *(перечень уточнён!)*
- **Суть:** хендлер вызывает `await callback.answer()`, затем `respond()`/`answer_callback()` вызывает `answer()` второй раз на тот же query. Если Telegram отклоняет повторный ответ, `callback.message.answer()` **не выполняется** — пользователь не получает экран. `answer_callback` не оборачивает `answer()` в try/except (`bot.py:142-147`).
- **Затронуты (проверено построчно):** `cb_add` (316→324/338), `cb_add_force` (348→352), `cb_history` (539→569), `cb_history_page` (546), `cb_settings` (672→676), `cb_export` (763→768/804), `cb_chart` (871→877/925), `cb_summary` (939→968), `cb_stats` (1043→1064).
- **⚠️ Уточнение к прошлому аудиту:** `cb_weekly` НЕ затронут (после `answer()` шлёт через `message.answer`, без второго ответа на query). Прошлый аудит его указывал ошибочно; зато пропускал add/history/settings.
- **Фикс (минимальный):** в `answer_callback` обернуть `await callback.answer()` в `try/except TelegramBadRequest: pass`. Правильнее — убрать дубли: `answer()` только в одном месте.

### 5. `del_confirm_*` без проверки прав
- **Где:** `bot.py:614-629`
- **Суть:** `cb_delete` проверяет `is_parent()`, а подтверждающий хендлер — нет. Защита должна быть на этапе выполнения действия, а не показа кнопки.
- **Фикс:** добавить `is_parent(callback.from_user.id)` в `cb_delete_confirm`.

---

## 🟠 Надёжность и корректность данных

### 6. Молчаливая перезапись времени суток
- **Где:** `bot.py:369-370` (`_save_measurement`)
- **Суть:** если слот `auto_time_of_day()` занят и `forced_tod` не задан, замер молча записывается как противоположное время суток. Замер в 10:30 при существующем утреннем попадёт в БД как «Вечер» → искажает статистику утро/вечер и недельные сравнения.
- **Фикс:** разрешать несколько замеров одного времени суток (счётчик вместо булевой логики «есть/нет») — частично уже реализовано через `add_force_`, но авто-путь всё ещё инвертирует. Явный вопрос пользователю при занятом слоте.

### 7. Блокирующие вызовы в async-хендлерах
- **Суть:** все SQLite-операции и генерация matplotlib-графика (`bot.py:888-923`) выполняются синхронно в event loop. Соединение открывается **на каждый запрос** (+ `PRAGMA journal_mode=WAL`) — 8–10 соединений на одно действие пользователя. При росте объёма бот замирает для всех.
- **Фикс:** `asyncio.to_thread()` для тяжёлых операций; долгоживущее соединение или `aiosqlite`.

### 8. Небезопасный парсинг callback_data
- **Где:** `bot.py:547, 587, 616, 638, 476, 489`
- **Суть:** `int(callback.data.replace(...))` без `try/except` — неожиданный payload роняет хендлер (и, без CI/тестов, никто не узнает).
- **Фикс:** хелпер `parse_int()` с fallback + алерт «Запись не найдена».

### 9. Состояние FSM «залипает»
- **Где:** `bot.py:1091-1096` (`catch_all`), `bot.py:714`
- **Суть:** во время `pef_input_*` ввод текста молча игнорируется (`if current: return`). В `editing_target_pef` нецифровой ввод не матчится `F.text.isdigit()` — тоже молчание. Пользователь не понимает, что делать.
- **Фикс:** хендлеры «нецифрового ввода» для всех состояний: подсказка + кнопка отмены. Плюс `/cancel` команда.

### 10. Дрейф схемы БД — **подтверждено по прод-БД**
- **Факт:** в `peakflow.db` таблица `measurements` создана старым кодом: БЕЗ `CHECK(time_of_day IN (...))`, С колонкой `note TEXT` (кодом не используется) и `FOREIGN KEY (user_id) REFERENCES users(user_id)`; таблицы `users` (3 строки) и `parent_child_links` (1 строка) — legacy, кодом не используются. Текущий `init_db()` о них не знает. Миграции — через `try/except ALTER`, без версионирования.
- **Фикс:** `PRAGMA user_version` + явные миграции; legacy-таблицы либо использовать (см. Фаза 4, мультидет), либо задокументировать и вычистить.

### 11. Нет индексов + LIKE-запросы
- **Факт:** в прод-БД только автоиндексы PK; все выборки фильтруют `user_id` + `measured_at` через `LIKE 'YYYY-MM-DD%'` (`database.py:153, 164`) — индекс по префиксу LIKE всё равно не используется.
- **Фикс:** `CREATE INDEX IF NOT EXISTS idx_meas_user_time ON measurements(user_id, measured_at)`; заменить LIKE на диапазон `>= today AND < tomorrow`.

---

## 🟡 Качество кода

### 12. Дублирование SQL в обход `database.py`
- **Где:** `_save_edit_any` (`bot.py:451-456`), `cb_edit_any` (`bot.py:589-592`), `cb_delete` (`bot.py:639-642`), `cb_delete_confirm` (`bot.py:617-621`) — прямой `sqlite3.connect()` в `bot.py`, хотя в `database.py` есть `edit_measurement`/`delete_measurement`. При этом `delete_measurement` и `get_week_measurements` нигде не используются (мёртвый код).
- **Фикс:** весь SQL — в `database.py`; добавить функции `get_measurement_by_id()`, `update_measurement_as_parent()`, `delete_measurement_as_parent()`; мёртвое удалить.

### 13. Два `get_effective_target()`
- **Где:** `bot.py:105` затеняет импортированный из `config.py:69`. Плюс `except (ValueError, Exception)` — бессмысленный кортеж (Exception уже покрывает ValueError).
- **Фикс:** одна реализация с чтением из `settings` (БД), импорт из config убрать, `except` упростить.

### 14. `TZ_OFFSET` читается в двух местах независимо
- **Где:** `config.py:11` и `database.py:7` — оба читают env напрямую. Правка одного не затронет другое. Ручное смещение не учитывает DST.
- **Фикс:** единый источник (config → импорт в database); долгосрочно — `zoneinfo` (`Europe/Yekaterinburg`).

### 15. Мёртвый код и недостижимые элементы *(перечень подтверждён)*
- `safe_delete` (`bot.py:156`), `_show_history_from_message` (`bot.py:817`), `Measurement.waiting_delete_confirm` (`bot.py:98`) — не используются.
- `PEF_NORM_BY_AGE` (`config.py:33`) — справочник не задействован.
- `ZONE_RED = 50` (`config.py:44`) — в логике зон не используется; красная зона фактически `< ZONE_YELLOW` (60%). Документация (README, wiki §18) описывает порог как `< 60%` — сверить с врачом, какая граница правильная, и привести код к единому виду.
- Хендлер `cb_stats` (`bot.py:1041`) **недостижим**: ни одна клавиатура не отправляет callback `stats`.
- На шаге десятков НЕТ кнопки «Назад» (`kb_pef_tens`, `bot.py:234`), хотя хендлер back для этого состояния написан (`bot.py:508-512`) — промахнувшийся пользователь застревает.
- **Фикс:** удалить мёртвое, добавить кнопку «Назад», `stats` либо убрать, либо дать кнопку ребёнку.

### 16. Markdown на пользовательских данных
- **Суть:** `parse_mode="Markdown"` везде, `CHILD_NAME` подставляется из env — `_` или `*` в имени сломают рендер.
- **Фикс:** HTML (`DefaultBotProperties(parse_mode=ParseMode.HTML)`) + `html.escape()`.

### 17. CSV-экспорт без экранирования
- **Где:** `bot.py:774-787`
- **Суть:** ручная склейка строк — запятая/кавычка в `CHILD_NAME` или имени сломает файл.
- **Фикс:** модуль `csv` с `QUOTE_ALL`.

---

## 🔵 Репозиторий, инфраструктура, безопасность

### 18. Секреты *(ситуация изменилась — частично решено)*
- ✅ `.env` и `*.db` теперь в `.gitignore` и в GitHub **не попали** (проверено по индексу коммита).
- ⚠️ **Новое:** в `README.md:24`, `readme-ru.md:59`, `readme-en.md:59` в примерах стоит **префикс реального токена** (`8565342225:AAF...`) — совпадает с первой частью боевого токена из `.env`. Заменить на плейсхолдер `123456:ABC-your-token`.
- ⚠️ Токен стоит ротировать через @BotFather — префикс уже был засвечен в доках до публикации.
- `.env` и прод-БД по-прежнему лежат в рабочей папке — это нормально при private-репо, но копий БД нет (см. №23).

### 19. Зависимости и окружение
- **Факт:** в текущем окружении aiogram, matplotlib и pytest **не установлены** — тесты запустить невозможно. `requirements.txt`: 3 пакета без пинов (`aiogram>=3.0.0` ломается между минорками 3.x), pytest не указан, хотя тесты — часть проекта.
- **Фикс:** `requirements.txt` с пинами + `requirements-dev.txt` (pytest) либо `pyproject.toml`.

### 20. Нет LICENSE-файла
- **Факт:** бейджи `License MIT` в `readme-ru.md:13` и `readme-en.md:13` ссылаются на файл, которого нет.
- **Фикс:** добавить `LICENSE` (MIT) — 5 минут.

### 21. Нет CI
- **Фикс:** GitHub Actions: `pip install -r requirements-dev.txt` + `pytest` на push/PR. После фикса №19 заработает сразу.

### 22. Git-идентичность
- **Факт:** автор initial-коммита — `root <root@claude-code.lan>` (авто-подстановка). Для истории репозитория настроить `git config user.name/user.email`.

### 23. Развёртывание и бэкапы
- **Факт:** запуск ручной (`python bot.py`), systemd-юнита/Dockerfile нет. Автоперезапуск при падении отсутствует, а падать есть на что (№8). Прод-БД с 183 медицинскими записями — **в одном экземпляре, без бэкапов**.
- **Фикс:** systemd-сервис с `Restart=on-failure`; cron с `sqlite3 .backup`; (быстро) кнопка «📥 Скачать бэкап» родителям.
- **Сделано (SP1, 2026-09-13):** systemd-сервис `peakflow-bot` (`Restart=on-failure`, `RestartSec=10`) ставится через `./manage.sh install`; `manage.sh backup`/`restore` делают `sqlite3 .backup` БД + `.env` в `backups/` (хранятся последние 10); добавлен health-check `GET /healthz` (используется `status`/`doctor`/`update`); `./manage.sh update` — обновление с GitHub, бэкапом и откатом при неудачном health-check; HTTPS для Mini App через `deploy/Caddyfile` и `./manage.sh caddy` (Let's Encrypt). SP2a (Mini App чтение: статус/история/график/статистика) — сделано 2026-09-13; запись (SP2b: add/edit/delete замеров и заметки с уведомлениями) — сделано 2026-09-14; настройки/CSV/бэкап (SP2c) — сделано 2026-09-14 (Mini App полностью паритетен боту).

### 24. Мусор в рабочей папке
- **Факт:** лежит `test_peakflow.db-journal` — фикстура тестов (`test/test_bot.py`) чистит `-wal`/`-shm`, но не `-journal`.
- **Фикс:** чистить все `*.db*` в фикстуре; файл удалить.
- **Сделано (SP1, 2026-09-13):** рантайм-мусор (`backups/`, `logs/`, `bot.pid`, `data/`) исключён через `.gitignore`.

---

## 📚 Документация и тесты

### 25. PROJECT.md описывает устаревшую версию
- Текстовый ввод ПСВ «50–800» и state `waiting_for_pef` (реально: пошаговый inline-ввод 100–690, states `pef_input_hundreds`/`pef_input_tens`); эмодзи 🌅/🌆 (в коде ☀️/🌙); «19 тестов» (фактически **24**); путь `/root/bot/picklo`; нет описаний настроек, CSV-экспорта, удаления записей, TZ_OFFSET.
- **Фикс:** переработать по образцу `wiki.md` (который актуален) или сократить до ссылки на wiki.

### 26. README-файлы устарели
- «Быстрый ввод: кнопка → число → готово» (реально: две клавиатуры сотни→десятки); «Диапазон: 50–800»; в таблицах переменных нет `TZ_OFFSET`; «19 тестов»; структура «picklo/»; бейджи Tests-19.
- **Фикс:** синхронизировать три README с текущим поведением (или оставить один, остальные — ссылки).

### 27. Тесты: покрытие и воспроизводимость
- **Факт (проверено):** 24 теста, но они импортируют `bot.py` целиком → требуют установленный aiogram и `BOT_TOKEN` валидного формата (иначе `Bot(token="")` падает при импорте на чистой машине). Логика FSM/хендлеров не покрыта вообще; регрессионного теста на баг №1 нет; фикстура не чистит `-journal`.
- **Фикс:** вынести логику в модули без сайд-эффектов при импорте; фейковые объекты aiogram для тестов хендлеров; регрессионный тест: «родитель редактирует последнюю запись» (упадёт сейчас, пройдёт после №1); чистить все суффиксы БД.

---

## 🗺️ План работ

### Фаза 0 — хотфиксы (~2 часа)
1. №1 — починить `edit_last` для родителей (+ регрессионный тест №27).
2. №2 — `ON CONFLICT DO NOTHING` в `mark_reminder_sent`.
3. №3 — окно планировщика `minute < 2`.
4. №4 — `try/except TelegramBadRequest` в `answer_callback` (минимальный фикс).
5. №5 — `is_parent` в `cb_delete_confirm`.
6. №18 — заменить префиксы токена в README на плейсхолдеры.
7. №15 — добавить кнопку «⬅️ Назад» в `kb_pef_tens`.

### Фаза 1 — надёжность данных (полдня)
8. №8 — безопасный парсинг callback_data; №9 — обработка нецифрового ввода + `/cancel`.
9. №6 — честная логика повторных замеров (без инверсии утро/вечер).
10. №11 — индекс `idx_meas_user_time` + диапазонные запросы вместо LIKE.
11. №7 — `asyncio.to_thread` для SQLite и matplotlib.
12. №15 — сверить порог красной зоны с врачом (50% или 60%) и привести код/доки к единому виду.

### Фаза 2 — чистка кода (полдня)
13. №12 — весь SQL через `database.py`; удалить мёртвые функции и хендлеры.
14. №13, №14 — один `get_effective_target`, один источник TZ.
15. №16 — HTML вместо Markdown; №17 — CSV через модуль `csv`.

### Фаза 3 — инфраструктура, тесты, документация (день)
16. №19 — пины зависимостей + `requirements-dev.txt`; №21 — GitHub Actions с pytest.
17. №10 — версионированные миграции (`PRAGMA user_version`), судьба legacy-таблиц.
18. №27 — тесты хендлеров и FSM; починка фикстуры (№24).
19. №25, №26 — актуализация PROJECT.md и README.
20. №20 — LICENSE; №22 — git-конфиг; №23 — systemd + бэкапы БД.

### Фаза 4 — развитие (по желанию)
21. Меню ребёнка: график и своя статистика (сейчас у ребёнка одна кнопка).
22. Напоминания самому ребёнку (сейчас — только родителям), эскалация родителям при отсутствии реакции.
23. Настройка времени напоминаний через ⚙️ (таблица `settings` уже используется для цели).
24. Заметки к замеру — колонка `note` уже в прод-БД («болел», «после спорта», «забыл лекарство»).
25. Мультидет — таблицы `users` и `parent_child_links` уже есть и заполнены; сейчас бот жёстко привязан к одному `CHILD_ID`.
26. PDF-отчёт врачу (matplotlib уже подключён) + экспорт «за период».
27. `aiosqlite`/долгоживущее соединение, `zoneinfo`, периодический `.backup` по кнопке.

---

## 💡 Быстрые победы (польза/усилие)

| # | Действие | Время |
|:-:|----------|:-----:|
| 1 | Кнопка «Назад» на шаге десятков (№15) | ~15 мин |
| 2 | LICENSE-файл (№20) | ~5 мин |
| 3 | Плейсхолдеры токена в README (№18) | ~10 мин |
| 4 | Пины зависимостей + requirements-dev (№19) | ~15 мин |
| 5 | try/except в answer_callback (№4) | ~10 мин |
| 6 | Регрессионный тест на edit_last (№27→№1) | ~30 мин |
| 7 | Индекс в БД (№11) | ~10 мин |

---

## ✅ Что уже хорошо

- WAL-режим; параметризованные SQL-запросы — инъекций нет.
- Singleton-lock через `flock` — защита от двойного запуска.
- Часовой пояс учтён везде (не только в планировщике).
- Проверка ID на `/start` — посторонние не пользуются ботом.
- Валидация диапазонов (цель 100–800; ввод ПСВ ограничен клавиатурами 100–690).
- Пошаговый inline-ввод без набора текста — удобно ребёнку.
- Дисклеймер «не медицинское устройство» во всех доках.
- Секреты исключены из репозитория; `.env.example`, `.gitignore`, `wiki.md` на месте; репозиторий приватный.

---

> Приоритет выполнения: **Фаза 0 целиком** — баги №1–5 влияют на ежедневную работу (родители не могут исправить замер, напоминания могут теряться, кнопки могут не отвечать). Остальное — по мере сил.