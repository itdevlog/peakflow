# SP5A — Интерактивный график и UX-мелочи Mini App: план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Добавить в график Mini App переключение вида (линия/столбцы/точки), диапазон (неделя/месяц), полный тултип с заметкой, и три UX-мелочи (busy-guard заметки, кэш месяцев, баннер истёкшего initData).

**Architecture:** Всё на текущем canvas без библиотек. `/api/chart` начинает отдавать `note` в точках. `drawChart` получает тип; диапазон фильтрует точки на клиенте; `chartClick` показывает расширенный тултип; `state` хранит `chartType`/`chartRange`/`months`. `showError("Нет доступа")` переключается на баннер. Новых Python-модулей и зависимостей нет.

**Tech Stack:** vanilla JS, canvas 2D, FastAPI, pytest.

**Spec:** `docs/superpowers/specs/2026-09-21-sp5a-chart-design.md`

## Global Constraints

- Без новых зависимостей и без CDN/графических библиотек.
- Canvas-рендер остаётся (никаких внешних чартов).
- `note` в `/api/chart` — только для активного ребёнка своей семьи (изоляция сохраняется).
- Пустая/отсутствующая заметка → `""`, тултип не ломается.
- Существующие chart-тесты и изоляция остаются зелёными.
- Тесты: `venv/bin/python -m pytest test/ -q`; standalone `venv/bin/python -m pytest test/test_webapp_api.py -q`; без `.env`.
- Полная проверка: `venv/bin/python -m pytest test/ -q && venv/bin/python -m pytest test/test_webapp_api.py -q && venv/bin/python -m pyflakes bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web/*.py scripts/*.py && venv/bin/python -m compileall -q bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web && echo ALL_GREEN`.
- Ветка: `sp5a-chart`; коммиты `feat(web): ... (SP5A)`.

---

## File Structure

- `web/api.py` — `note` в точках `/api/chart`.
- `web/static/index.html` — `#chart-controls` (типы+диапазоны), `#auth-hint`.
- `web/static/app.js` — типы/диапазон/тултип/busy-guard/кэш/баннер.
- `test/test_webapp_api.py` — API-тест на `note` + статические проверки JS/HTML.
- `PROJECT.md`, `wiki.md`, `docs/superpowers/specs/2026-09-17-todo-plan.md` — счётчики/статус.

---

### Task 1: `/api/chart` отдаёт заметку

**Files:** Modify `web/api.py`; Test `test/test_webapp_api.py`.

**Interfaces:**
- Produces: в каждом элементе `points` поле `"note": str`.

- [x] **Step 1: Write the failing test** (append to `test/test_webapp_api.py`)

```python
class TestChartNote:
    def test_chart_includes_note(self):
        _setup_db()
        conn = sqlite3.connect(TEST_DB)
        conn.execute(
            "INSERT INTO measurements (family_id, child_id, pef_value, time_of_day, "
            "measured_at, added_by, source, note) VALUES (1, ?, 250, 'morning', ?, ?, "
            "'manual', 'болел')",
            (CHILD_ID, datetime.now().strftime("%Y-%m-%d %H:%M:%S"), CHILD_ID))
        conn.commit()
        conn.close()
        body = _client().get("/api/chart", headers=_auth(CHILD_ID)).json()
        assert body["points"][0]["note"] == "болел"

    def test_chart_note_empty_string(self):
        _setup_db()
        add_measurement(TEST_DB, 250, "morning", CHILD_ID, CHILD_ID, family_id=1)
        body = _client().get("/api/chart", headers=_auth(CHILD_ID)).json()
        assert body["points"][0]["note"] == ""
```

- [x] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestChartNote -q`
Expected: FAIL — `KeyError: 'note'`.

- [x] **Step 3: Write minimal implementation**

In `web/api.py` `chart`, add the field to the point dict:

```python
        points = [
            {
                "date": str(r["measured_at"])[:10],
                "tod": r["time_of_day"],
                "pef": r["pef_value"],
                "source": r.get("source") or "manual",
                "note": r.get("note") or "",
            }
            for r in rows
        ]
```

- [x] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestChartNote test/test_webapp_api.py -q`
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add web/api.py test/test_webapp_api.py
git commit -m "feat(web): include note in chart points (SP5A)"
```

---

### Task 2: Контролы графика — тип и диапазон

**Files:** Modify `web/static/index.html`, `web/static/app.js`; Test `test/test_webapp_api.py`.

**Interfaces:**
- Produces: `state.chartType` (`"line"|"bars"|"points"`), `state.chartRange` (`"week"|"month"`), `state.months`; `visiblePoints(data)`, `syncChartControls()`, `redrawChart()`; `drawChart(data, points, type)`.

- [x] **Step 1: Write the failing test** (append to `test/test_webapp_api.py`)

```python
class TestMiniAppChartUi:
    def _js(self):
        import pathlib
        return pathlib.Path("web/static/app.js").read_text(encoding="utf-8")

    def _html(self):
        import pathlib
        return pathlib.Path("web/static/index.html").read_text(encoding="utf-8")

    def test_controls_present(self):
        html = self._html()
        assert "chart-controls" in html
        assert 'data-chart-type="line"' in html
        assert 'data-chart-type="bars"' in html
        assert 'data-chart-type="points"' in html
        assert 'data-chart-range="week"' in html
        assert 'data-chart-range="month"' in html

    def test_state_and_helpers(self):
        js = self._js()
        for token in ("chartType", "chartRange", "state.months",
                      "visiblePoints", "syncChartControls", "redrawChart"):
            assert token in js
```

- [x] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestMiniAppChartUi -q`
Expected: FAIL — controls/helpers absent.

- [x] **Step 3: Implement**

`web/static/index.html` — inside `#screen-chart`, after `#chart-nav`:

```html
      <div id="chart-controls">
        <div class="seg">
          <button class="mini" data-chart-type="line">Линия</button>
          <button class="mini" data-chart-type="bars">Столбцы</button>
          <button class="mini" data-chart-type="points">Точки</button>
        </div>
        <div class="seg">
          <button class="mini" data-chart-range="week">Неделя</button>
          <button class="mini" data-chart-range="month">Месяц</button>
        </div>
      </div>
```

`web/static/app.js`:

- extend `state` (line ~23):

```javascript
  chart: { year: null, month: null }, chartData: null, history: { page: 1 },
  chartType: "line", chartRange: "month", months: [],
```

- replace `drawChart` body from the line-drawing section onward so it honours `type`. After the target line / axes / ticks blocks, replace the line+markers block with:

```javascript
  if (type === "bars") {
    const bw = Math.max(3, Math.min(20, (plotW / Math.max(1, n)) * 0.6));
    points.forEach((p, i) => {
      const x = xToPx(i), y = yToPx(p.pef);
      ctx.fillStyle = p.tod === "morning" ? "#e8a600" : "#7a5cff";
      ctx.fillRect(x - bw / 2, y, bw, (cssH - padB) - y);
    });
  } else {
    if (type === "line" && n >= 2) {
      ctx.strokeStyle = "#555"; ctx.lineWidth = 1.5; ctx.beginPath();
      points.forEach((p, i) => { const x = xToPx(i), y = yToPx(p.pef);
        i ? ctx.lineTo(x, y) : ctx.moveTo(x, y); });
      ctx.stroke();
    }
    ctx.lineWidth = 1;
    points.forEach((p, i) => {
      ctx.fillStyle = p.tod === "morning" ? "#e8a600" : "#7a5cff";
      ctx.beginPath(); ctx.arc(xToPx(i), yToPx(p.pef), 3.5, 0, Math.PI * 2); ctx.fill();
    });
  }
```

Change the signature to `function drawChart(data, points, type) {`.

- add helpers after `drawChart`:

```javascript
function visiblePoints(data) {
  const pts = (data && data.points) || [];
  if (state.chartRange !== "week" || !pts.length) return pts;
  const lastMs = Date.parse(pts[pts.length - 1].date + "T00:00:00");
  const cutoff = lastMs - 6 * 86400000;
  return pts.filter((p) => Date.parse(p.date + "T00:00:00") >= cutoff);
}

function syncChartControls() {
  document.querySelectorAll("[data-chart-type]").forEach((b) =>
    b.classList.toggle("active", b.dataset.chartType === state.chartType));
  document.querySelectorAll("[data-chart-range]").forEach((b) =>
    b.classList.toggle("active", b.dataset.chartRange === state.chartRange));
}

function redrawChart() {
  const d = state.chartData;
  if (!d) return;
  $("chart-tip").hidden = true;
  drawChart(d, visiblePoints(d), state.chartType);
  syncChartControls();
}
```

- update `loadChart`:
  - after `state.chart = {...}`: `state.months = data.available_months || [];`
  - add `state.chartRange = "month";` (reset on month change)
  - empty branch: `$("chart-controls").style.display = "none";`
  - non-empty branch: `$("chart-controls").style.display = "";` then `state.chartData = data; redrawChart();`
- update the resize handler to call `redrawChart()` instead of `drawChart(d, d.points)`.
- add control wiring next to the `chart-prev`/`chart-next` wiring:

```javascript
document.querySelectorAll("[data-chart-type]").forEach((b) =>
  b.onclick = () => { state.chartType = b.dataset.chartType; redrawChart(); });
document.querySelectorAll("[data-chart-range]").forEach((b) =>
  b.onclick = () => { state.chartRange = b.dataset.chartRange; redrawChart(); });
```

- [x] **Step 4: Run tests**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestMiniAppChartUi test/test_webapp_api.py -q`
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add web/static/index.html web/static/app.js test/test_webapp_api.py
git commit -m "feat(web): chart type and range controls (SP5A)"
```

---

### Task 3: Полный тултип (значение, %, зона, заметка)

**Files:** Modify `web/static/app.js`; Test `test/test_webapp_api.py`.

**Interfaces:**
- Consumes (Task 2): `state.chartData`, `drawChart` sets `c._points`.
- Produces: `pointZone(p, data) -> {pct, emoji}`; `chartClick` показывает дату/период/значение/%/зону/заметку.

- [x] **Step 1: Write the failing test** (append to `TestMiniAppChartUi`)

```python
    def test_tooltip_full(self):
        js = self._js()
        assert "pointZone" in js
        assert "p.note" in js
        assert "л/мин" in js
```

- [x] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestMiniAppChartUi::test_tooltip_full -q`
Expected: FAIL — `pointZone` absent.

- [x] **Step 3: Implement**

In `web/static/app.js`, add before `chartClick`:

```javascript
function pointZone(p, data) {
  const t = (data && data.target_pef) || 0;
  if (!t) return { pct: null, emoji: "" };
  const pct = Math.round((p.pef / t) * 100);
  const zones = (data && data.zones) || {};
  if (pct >= (zones.green || 80)) return { pct, emoji: "🟢" };
  if (pct >= (zones.yellow || 60)) return { pct, emoji: "🟡" };
  return { pct, emoji: "🔴" };
}
```

Replace `chartClick` with:

```javascript
function chartClick(ev) {
  const c = $("chart");
  const pts = c._points || [];
  const data = state.chartData || {};
  const rect = c.getBoundingClientRect();
  const x = ev.clientX - rect.left, y = ev.clientY - rect.top;
  let best = null, bestD = 1e9;
  for (const q of pts) {
    const d = (q.x - x) ** 2 + (q.y - y) ** 2;
    if (d < bestD) { bestD = d; best = q; }
  }
  const tip = $("chart-tip");
  if (best && bestD < 900) {
    const p = best.p;
    const z = pointZone(p, data);
    let text = `${p.date} · ${todLabel(p.tod)} · ${p.pef} л/мин`;
    if (z.pct != null) text += ` · ${z.pct}% ${z.emoji}`;
    if (p.note) text += `\n📝 ${p.note}`;
    tip.hidden = false;
    tip.textContent = text;
  } else { tip.hidden = true; }
}
```

- [x] **Step 4: Run test**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestMiniAppChartUi -q`
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add web/static/app.js test/test_webapp_api.py
git commit -m "feat(web): full chart tooltip with zone and note (SP5A)"
```

---

### Task 4: UX-мелочи (busy-guard, кэш месяцев, баннер initData)

**Files:** Modify `web/static/index.html`, `web/static/app.js`; Test `test/test_webapp_api.py`.

**Interfaces:**
- Produces: `showAuthHint()`; `showError` перехватывает «Нет доступа»; `shiftMonth` использует `state.months`; `#note-save` блокируется на время POST.

- [x] **Step 1: Write the failing test** (append to `TestMiniAppChartUi`)

```python
    def test_ux_items(self):
        js = self._js()
        html = self._html()
        assert "auth-hint" in html and "auth-hint" in js
        assert "Нет доступа" in js
        assert "state.months" in js
        assert "note-save" in js and "disabled" in js
```

- [x] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestMiniAppChartUi::test_ux_items -q`
Expected: FAIL — `auth-hint` absent.

- [x] **Step 3: Implement**

`web/static/index.html` — add near `#error`:

```html
  <div id="auth-hint" hidden>Откройте приложение заново из Telegram</div>
```

`web/static/app.js`:

- replace `showError` and add the hint helper:

```javascript
function showAuthHint() {
  if (showAuthHint._shown) return;
  showAuthHint._shown = true;
  const el = $("auth-hint");
  if (el) el.hidden = false;
}

function showError(msg) {
  if (msg === "Нет доступа") { showAuthHint(); return; }
  const el = $("error");
  el.textContent = msg;
  el.hidden = false;
}
```

- busy-guard in `openNote`:

```javascript
  $("note-save").onclick = async () => {
    const btn = $("note-save");
    if (btn.disabled) return;
    btn.disabled = true;
    btn.textContent = "Сохраняем…";
    const text = $("note-input").value;
    try {
      await api(`/api/measurements/${mid}/note`, { method: "POST", body: { note: text } });
      closeForm();
      await switchTo("today");
    } catch (e) {
      showError(e.message);
    } finally {
      btn.disabled = false;
      btn.textContent = "Сохранить";
    }
  };
```

- month cache in `shiftMonth`:

```javascript
async function shiftMonth(delta) {
  if (!state.months.length) {
    const data = await api(`/api/chart?year=${state.chart.year}&month=${state.chart.month}`);
    state.months = data.available_months || [];
  }
  const months = state.months;
  const cur = `${state.chart.year}-${state.chart.month}`;
  const union = months.includes(cur) ? months.slice() : [...months, cur].sort();
  const idx = union.indexOf(cur);
  const nextIdx = idx + delta;
  if (nextIdx < 0 || nextIdx >= union.length) return;
  const target = union[nextIdx];
  if (target === cur) return;
  const [y, m] = target.split("-");
  await loadChart(Number(y), Number(m));
}
```

- [x] **Step 4: Run tests**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestMiniAppChartUi test/test_webapp_api.py -q`
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add web/static/index.html web/static/app.js test/test_webapp_api.py
git commit -m "feat(web): note busy-guard, month cache, auth hint (SP5A)"
```

---

### Task 5: Документация, счётчики, полная регрессия

**Files:** `PROJECT.md`, `wiki.md`, `docs/superpowers/specs/2026-09-17-todo-plan.md`.

- [x] **Step 1: `PROJECT.md`** — типы графика, диапазон, тултип с заметкой (`note` в `/api/chart`), busy-guard/кэш/баннер; обновить число тестов.

- [x] **Step 2: `wiki.md`** — то же по существу; обновить число тестов.

- [x] **Step 3: todo-plan** — отметить `5.1` и `5.6` как выполненные (✅ SP5A); обновить тестовые счётчики (шапка + «Метрики успеха»).

- [x] **Step 4: Run full verification**

Run:
```bash
venv/bin/python -m pytest test/ -q && venv/bin/python -m pytest test/test_webapp_api.py -q && venv/bin/python -m pyflakes bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web/*.py scripts/*.py && venv/bin/python -m compileall -q bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web && echo ALL_GREEN
```
Expected: `ALL_GREEN`; зафиксировать фактическое число тестов. Дополнительно `CHILD_ID=0 PARENT_IDS=0,0 CHILD_NAME=Ребёнок venv/bin/python -m pytest test/ -q`.

- [x] **Step 5: Commit**

```bash
git add PROJECT.md wiki.md docs/superpowers/specs/2026-09-17-todo-plan.md
git commit -m "docs: SP5A interactive chart and Mini App UX (SP5A)"
```

---

## Self-Review

**1. Spec coverage:**
- Типы графика (линия/столбцы/точки) → Task 2 ✅
- Диапазон неделя/месяц (клиентский фильтр) → Task 2 ✅
- Полный тултип (значение/дата/%/зона/заметка) + `note` в API → Tasks 1, 3 ✅
- busy-guard заметки, кэш месяцев, баннер initData → Task 4 ✅
- Документация/счётчики/регрессия → Task 5 ✅
- Вне scope (pinch/pan, межмесячный диапазон, библиотеки) — не включено ✅

**2. Placeholder scan:** плейсхолдеров нет; все шаги содержат код/точные команды.

**3. Type consistency:**
- `state.chartType`/`state.chartRange`/`state.months` — Task 2; используются в Tasks 2/3/4.
- `drawChart(data, points, type)` — Task 2; `visiblePoints(data)`, `syncChartControls()`, `redrawChart()` — Task 2.
- `pointZone(p, data) -> {pct, emoji}` — Task 3.
- `showAuthHint()` — Task 4; `note` в точках API — Task 1, потребляется Task 3.
