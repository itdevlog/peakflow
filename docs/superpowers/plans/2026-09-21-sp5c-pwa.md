# SP5C — PWA: установка и офлайн app-shell: план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Сделать Mini App устанавливаемым (манифест + SVG-иконка) и устойчивым к офлайну (service worker кэширует app-shell), не кэшируя данные API.

**Architecture:** `web/static/manifest.json` + `icon.svg`, линковка и регистрация SW в `index.html`, и `web/static/sw.js` с версионированным кэшем (precache shell, `skipWaiting`/`clients.claim`, network-only для `/api/`, `/healthz`, `/metrics`, network-first для навигации). Всё — progressive enhancement, без библиотек.

**Tech Stack:** vanilla JS, Service Worker / Cache API, FastAPI StaticFiles, pytest.

**Spec:** `docs/superpowers/specs/2026-09-21-sp5c-pwa-design.md`

## Global Constraints

- Без новых зависимостей и без workbox/CDN.
- Данные замеров (`/api/`, `/healthz`, `/metrics`) **никогда** не кэшируются.
- Кэшируется только app-shell (статика).
- SW-регистрация обёрнута в `catch` и guard `"serviceWorker" in navigator`.
- Тесты: `venv/bin/python -m pytest test/ -q`; standalone `venv/bin/python -m pytest test/test_webapp_api.py -q`; без `.env`.
- Полная проверка: `venv/bin/python -m pytest test/ -q && venv/bin/python -m pytest test/test_webapp_api.py -q && venv/bin/python -m pyflakes bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web/*.py scripts/*.py && venv/bin/python -m compileall -q bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web && echo ALL_GREEN`.
- Ветка: `sp5c-pwa`; коммиты `feat(web): ... (SP5C)`.

---

## File Structure

- `web/static/manifest.json` (новый).
- `web/static/icon.svg` (новый).
- `web/static/sw.js` (новый).
- `web/static/index.html` — ссылки + регистрация SW.
- `test/test_webapp_api.py` — статические проверки.
- `PROJECT.md`, `wiki.md`, `docs/superpowers/specs/2026-09-17-todo-plan.md` — счётчики/статус.

---

### Task 1: Манифест, иконка, линковка и регистрация SW

**Files:** Create `web/static/manifest.json`, `web/static/icon.svg`; Modify `web/static/index.html`; Test `test/test_webapp_api.py`.

**Interfaces:**
- Produces: PWA-манифест `/manifest.json`, иконка `/icon.svg`, регистрация `/sw.js` в `index.html`.

- [ ] **Step 1: Write the failing test** (append to `test/test_webapp_api.py`)

```python
class TestPwaInstall:
    def test_manifest_valid(self):
        import json
        import pathlib
        data = json.loads(pathlib.Path("web/static/manifest.json").read_text(encoding="utf-8"))
        assert data["name"] and data["short_name"]
        assert data["start_url"]
        assert data["display"] == "standalone"
        assert data["icons"] and data["icons"][0]["type"] == "image/svg+xml"

    def test_icon_svg(self):
        import pathlib
        svg = pathlib.Path("web/static/icon.svg").read_text(encoding="utf-8")
        assert svg.lstrip().startswith("<svg")

    def test_index_links_and_registration(self):
        import pathlib
        html = pathlib.Path("web/static/index.html").read_text(encoding="utf-8")
        assert 'rel="manifest"' in html and "/manifest.json" in html
        assert 'name="theme-color"' in html
        assert "/icon.svg" in html
        assert 'navigator.serviceWorker.register("/sw.js")' in html
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestPwaInstall -q`
Expected: FAIL — `FileNotFoundError` / missing tokens.

- [ ] **Step 3: Implement**

Create `web/static/manifest.json`:

```json
{
  "name": "Пикфлоуметр",
  "short_name": "Пикфлоуметр",
  "description": "Дневник пикфлоуметрии для семьи",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#2ea6ff",
  "icons": [
    {"src": "./icon.svg", "sizes": "any", "type": "image/svg+xml", "purpose": "any maskable"}
  ]
}
```

Create `web/static/icon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" role="img" aria-label="Пикфлоуметр">
  <rect width="512" height="512" rx="112" fill="#2ea6ff"/>
  <path d="M96 320 L176 320 L208 224 L256 384 L304 256 L352 320 L416 320"
        fill="none" stroke="#ffffff" stroke-width="28" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
```

In `web/static/index.html` add to `<head>` (after the `style.css` link):

```html
<link rel="manifest" href="/manifest.json">
<meta name="theme-color" content="#2ea6ff">
<link rel="icon" type="image/svg+xml" href="/icon.svg">
```

And before `</body>` (after `<script src="app.js"></script>`):

```html
<script>
if ("serviceWorker" in navigator) {
  window.addEventListener("load", () => {
    navigator.serviceWorker.register("/sw.js").catch(() => {});
  });
}
</script>
```

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestPwaInstall -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add web/static/manifest.json web/static/icon.svg web/static/index.html test/test_webapp_api.py
git commit -m "feat(web): PWA manifest, icon and service worker registration (SP5C)"
```

---

### Task 2: Service worker `sw.js`

**Files:** Create `web/static/sw.js`; Test `test/test_webapp_api.py`.

**Interfaces:**
- Consumes: shell assets from Task 1.
- Produces: `/sw.js` — versioned app-shell cache with API bypass.

- [ ] **Step 1: Write the failing test** (append to `test/test_webapp_api.py`)

```python
class TestServiceWorker:
    def test_sw_source(self):
        import pathlib
        js = pathlib.Path("web/static/sw.js").read_text(encoding="utf-8")
        for token in ("peakflow-v", "/api/", "skipWaiting", "clients.claim",
                      "addAll", '"navigate"'):
            assert token in js, f"sw.js missing {token!r}"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestServiceWorker -q`
Expected: FAIL — `FileNotFoundError`.

- [ ] **Step 3: Implement**

Create `web/static/sw.js`:

```javascript
const CACHE = "peakflow-v1";
const SHELL = ["/", "/index.html", "/app.js", "/style.css", "/manifest.json", "/icon.svg"];
const BYPASS = ["/api/", "/healthz", "/metrics"];

self.addEventListener("install", (event) => {
  event.waitUntil(
    caches.open(CACHE)
      .then((cache) => cache.addAll(SHELL))
      .then(() => self.skipWaiting())
  );
});

self.addEventListener("activate", (event) => {
  event.waitUntil(
    caches.keys()
      .then((keys) => Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener("fetch", (event) => {
  const request = event.request;
  if (request.method !== "GET") return;
  const url = new URL(request.url);
  if (url.origin !== self.location.origin) return;
  if (BYPASS.some((p) => url.pathname.startsWith(p))) return;

  if (request.mode === "navigate") {
    event.respondWith(fetch(request).catch(() => caches.match("/index.html")));
    return;
  }

  event.respondWith(
    caches.match(request).then((cached) =>
      cached || fetch(request).then((response) => {
        const copy = response.clone();
        caches.open(CACHE).then((cache) => cache.put(request, copy));
        return response;
      })
    )
  );
});
```

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m pytest test/test_webapp_api.py::TestServiceWorker test/test_webapp_api.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add web/static/sw.js test/test_webapp_api.py
git commit -m "feat(web): offline app-shell service worker (SP5C)"
```

---

### Task 3: Документация, счётчики, полная регрессия

**Files:** `PROJECT.md`, `wiki.md`, `docs/superpowers/specs/2026-09-17-todo-plan.md`.

- [ ] **Step 1: `PROJECT.md`** — PWA (manifest, icon, `sw.js`, offline app-shell, API не кэшируется); обновить число тестов.
- [ ] **Step 2: `wiki.md`** — то же по существу; обновить число тестов.
- [ ] **Step 3: todo-plan** — отметить `5.3` как выполненный (✅ SP5C); обновить счётчики.
- [ ] **Step 4: Run full verification**

Run:
```bash
venv/bin/python -m pytest test/ -q && venv/bin/python -m pytest test/test_webapp_api.py -q && venv/bin/python -m pyflakes bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web/*.py scripts/*.py && venv/bin/python -m compileall -q bot.py database.py config.py report.py report_pdf.py gamification.py metrics.py web && echo ALL_GREEN
```
Expected: `ALL_GREEN`; зафиксировать фактическое число тестов. Дополнительно CI-подобно: `CHILD_ID=0 PARENT_IDS=0,0 CHILD_NAME=Ребёнок venv/bin/python -m pytest test/ -q`.

- [ ] **Step 5: Commit**

```bash
git add PROJECT.md wiki.md docs/superpowers/specs/2026-09-17-todo-plan.md
git commit -m "docs: SP5C PWA app-shell (SP5C)"
```

---

## Self-Review

**1. Spec coverage:**
- Манифест + SVG-иконка + standalone → Task 1 ✅
- Линковка и регистрация SW с guard/catch → Task 1 ✅
- SW: precache, версия, skipWaiting/clients.claim, чистка, API-байпас, навигация → Task 2 ✅
- Тесты статические + регрессия/счётчики → Tasks 1/2/3 ✅
- Вне scope (offline API, push, PNG, iOS, Caddy) — не включено ✅

**2. Placeholder scan:** плейсхолдеров нет; все шаги содержат файлы/код/команды.

**3. Type consistency:**
- `manifest.json` поля, `icon.svg`, `/sw.js` — Task 1 создаёт/линкует; Task 2 создаёт `sw.js`; Task 3 документирует.
- Пути `/manifest.json`, `/icon.svg`, `/sw.js` согласованы со `StaticFiles` в корне.
