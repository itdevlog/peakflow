# To-do план развития PeakFlow: от семейного бота к мульти-сервису

> **Дата:** 2026-09-17
> **Основание:** свежий аудит кода (bot.py, database.py, web/*, report.py, config.py) + стратегия из `roadmap.md` (Фазы 4–5)
> **Фокус:** трансформация в публичный мульти-сервис; найденные баги — обязательный подготовительный этап
> **Текущее состояние:** 519 тестов, схема v5, полный мульти-тенант (Фазы 0–2 в main)
> **Статус:** ✅ Фазы 0–2 выполнены (2026-09-17). **Следующая активная — Фаза 4 (польза пользователю).** Фаза 3 — «по требованию».

---

## ✅ Выполнено 2026-09-17 (Фазы 0–1)

### Фаза 0 — Хотфиксы

| # | Что сделано | Файлы |
|:-:|-------------|-------|
| 0.1 (A1) | `acquire_lock` держит file object в module-level `_lock_handle`; flock больше не снимается GC | `bot.py:74-111` |
| 0.2 (A2) | `is_parent` добавлен в `cb_export`, `cb_export_all`, `cb_export_month`, `cb_summary`, `cb_weekly`, `cb_edit_last` | `bot.py` |
| 0.3 (A3) | `add_or_replace_measurement` — атомарно под `BEGIN IMMEDIATE` (check + auto-replace + insert); web API переведён на неё | `database.py`, `web/api.py` |
| 0.4 (A5) | Уведомления → `BackgroundTasks`; `_SEND_TIMEOUT=5s` в notify (не держат HTTP-запрос) | `web/api.py`, `web/notify.py` |
| 0.5 (B3) | Red-zone не уходит автору замера (bot + web) | `bot.py`, `web/notify.py` |
| 0.6 (B1) | `_scheduler_task` с сильной ссылкой + `add_done_callback` для логов | `bot.py` |
| 0.7 (B7) | `is_parent` в `cb_edit_last` | `bot.py` |
| 0.8 (C1,C2) | Удалены мёртвые `REMINDER_*` константы, ветка `edit_last` в `cb_edit_any`, `ZONE_RED` из API; `config.py` почищен | `config.py`, `bot.py`, `web/api.py` |

### Фаза 1 — Производительность и надёжность

| # | Что сделано | Файлы |
|:-:|-------------|-------|
| 1.1-1.3 (A4,B10) | Все вызовы БД в async-хендлерах бота и API обёрнуты в `asyncio.to_thread` (helper `_db`); проверено AST-скриптом — 0 синхронных вызовов | `bot.py`, `web/api.py` |
| 1.2 (A4) | `get_recent_measurements(limit)` вместо полного `get_all_measurements` для статус-блока/диффа | `database.py`, `bot.py` |
| 1.4 (B2) | Тик выровнен по границе минуты (`seconds_until_next_minute`); флаг ставится только при успешной доставке — ретрай | `bot.py` |
| 1.5 (B5) | `try/finally` + `plt.close(fig)` — фигура не утекает при сбое рендера | `bot.py` |
| 1.6 (B6) | `_show_history` клампит устаревшую страницу в `total_pages` | `bot.py` |
| 1.7 (B4) | Удалён дубль `_show_history_from_callback`; используется единый `_show_history` | `bot.py` |
| 1.8 (B9) | `bot.session.close()` в ветке без `WEBAPP_PORT` | `bot.py` |
| 1.9 (B8,C4) | `/api/me` отдаёт `zones`; app.js читает пороги из конфига и использует `Math.floor` (как `int()` сервера) | `web/api.py`, `web/static/app.js` |
| 1.10 (C5) | Дифф считается с предыдущим замером того же `time_of_day` (`get_previous_of_tod`) | `database.py`, `bot.py`, `web/api.py` |
| 1.11 (C3) | Удалены мёртвые эндпоинты `/api/weekly`, `/api/summary` и их тесты | `web/api.py` |
| 1.12 (C7) | `validate_reminder_hours`: эскалация родителям строго после пинга ребёнку (bot + API) | `database.py`, `bot.py`, `web/api.py` |
| 1.13 | Нагрузочный smoke-тест: 100 конкурирующих записей сериализуются в 1 строку | `test/test_load.py` |

**Тесты:** 197 → **236** (+39), `pyflakes` и `compileall` чисто.

---

## 1. Результаты аудита 2026-09-17 (исходные находки)

### 🔴 Критично — блокирует путь к мульти-сервису

| # | Проблема | Где | Влияние |
|:-:|----------|-----|---------|
| A1 | **Singleton-lock не работает**: `acquire_lock()` возвращает `fileno`, а file object собирается GC → `flock` снимается сразу после выхода из функции. Защита от двойного запуска фиктивна | `bot.py:77-97` | Двойной запуск = конфликт `getUpdates`, дубли auto-записей и напоминаний (неатомарные пары check-then-act) |
| A2 | **Нет проверок роли** в `cb_export` (989), `cb_export_all` (1014), `cb_export_month` (1023), `cb_summary` (1264), `cb_weekly` (1306) — в отличие от settings/backup/delete | `bot.py` | Ребёнок через пересланную кнопку выгружает полный CSV/сводку |
| A3 | **Race в Mini App add**: `has_today_measurement` → вставка не атомарны (два соединения, без транзакции); двойной тап с разных устройств = дубли | `web/api.py:244-256` | Дубликаты замеров, искажение статистики |
| A4 | **Блокирующий SQLite во всех async-хендлерах** (бот и API). Худшее: `backup_db` + `f.read()` в `cb_backup`; `get_all_measurements` грузит всю историю ради диффа двух последних | `bot.py:265,280,428-438,1051-1062`; `web/api.py:166-356` | Event loop заморожен для бота И веба (uvicorn в том же loop) на время запроса к БД |
| A5 | **Уведомления awaited внутри HTTP-запроса** — латентность/недоступность Telegram напрямую задерживает ответ клиенту | `web/api.py:262-264`, `web/notify.py:27-31` | Mini App виснет при проблемах Telegram |

### 🟠 Средне — надёжность и корректность

| # | Проблема | Где |
|:-:|----------|-----|
| B1 | `asyncio.create_task(scheduler_loop())` без сильной ссылки — GC может собрать task, падение никто не заметит | `bot.py:1454` |
| B2 | Окно напоминания (`minute < 2`) пропускается без ретрая, если итерация заняла >2 мин; флаг ставится после try/except — при сбое доставки повтор только через сутки | `bot.py:1544-1577, 1486-1495, 1562-1572` |
| B3 | Red-zone алерт уходит ВСЕМ родителям, включая автора замера (информирующее уведомление автора исключает, красная зона — нет) | `bot.py:471-482`, `web/notify.py:54` |
| B4 | `_show_history_from_callback` — дубль `_show_history` без заметок (ℹ️) и авто-меток (🤖): после правки/удаления история выглядит иначе | `bot.py:1082-1104` vs `676-683` |
| B5 | Утечка matplotlib при исключении: между `plt.subplots` и `plt.close(fig)` нет try/finally | `bot.py:1145-1190` |
| B6 | `hist_page_N` не ограничен `total_pages` — после удаления записей устаревшая кнопка ведёт на пустую страницу | `bot.py:667-673` |
| B7 | `cb_edit_last` доступен ребёнку через кнопку из сообщения «уже измерено» (в главном меню у ребёнка её нет) | `bot.py:642, 364` |
| B8 | Захардкоженные пороги зон в клиенте (80/60) вместо серверных `zones` из `/api/chart` — при нестандартном конфиге цвета разойдутся | `web/static/app.js:13-17` |
| B9 | Ветка без `WEBAPP_PORT` не закрывает `bot.session` | `bot.py:1606-1608` |
| B10 | Uvicorn и aiogram в одном event loop без изоляции — усиливает A4 | `web/server.py:36-47` |

### 🟡 Низко — чистка и UX

| # | Проблема | Где |
|:-:|----------|-----|
| C1 | Мёртвые константы `REMINDER_MORNING_HOUR/DEADLINE/EVENING_*` в config.py; реальные дефолты дублируются в `database.py:452-457` (`REMINDER_HOURS_DEFAULT`) — два источника правды | `config.py:68-71` |
| C2 | Мёртвый код: ветка `edit_last` в `cb_edit_any` (714) недостижима; `ZONE_RED` отдаётся в `/api/chart`, но не используется | `bot.py:714`, `web/api.py:219` |
| C3 | Мёртвые эндпоинты `/api/weekly` (offset зажат в 0) и `/api/summary` не используются клиентом | `web/api.py:234-237` |
| C4 | Расхождение «Всего замеров»: settings считает `include_auto=True`, stats — без auto; округление: клиент `Math.round`, сервер `int()` — возможен цвет zone на границе | `web/api.py:293`, `app.js:80` |
| C5 | `diff` при повторном замере считается с предыдущей записью, а не с аналогом вчера — семантика «Изменение» плавает | `bot.py:441`, `web/api.py:260` |
| C6 | `note-save` без busy-guard (двойной клик = 2 POST); `shiftMonth` делает лишний запрос; истёкший initData (>24 ч) не подсказывает переоткрыть приложение | `app.js:453-460, 333-347, 556` |
| C7 | Нет валидации соотношения часов напоминаний (`parent_morning == child_morning` → пинг и эскалация в одну минуту) | `bot.py:897-901` |

> ✅ **Авторизация Mini App проверена — пропусков нет:** все эндпоинты кроме `/healthz` закрыты `require_user`/`require_parent`, HMAC-проверка корректна (DCS, `compare_digest`, свежесть `auth_date`, fail-closed).

---

## 2. To-do план по фазам

### ✅ Фаза 0 — Хотфиксы перед мульти-сервисом (ВЫПОЛНЕНО 2026-09-17)

### ✅ Фаза 1 — Производительность и надёжность (ВЫПОЛНЕНО 2026-09-17)

### 🔵 Фаза 2 — Мульти-тенантная архитектура (месяцы 1-2)

#### ✅ Подпроект 2A — Мульти-тенантная схема v2 (ВЫПОЛНЕНО 2026-09-17)

- Spec: `docs/superpowers/specs/2026-09-17-sp3a-multi-tenant-schema-design.md`
- Plan: `docs/superpowers/plans/2026-09-17-sp3a-multi-tenant-schema.md`
- Сделано: таблицы `families`/`members`; `measurements.child_id` вместо `user_id`
  + `family_id`; `settings` по `(family_id, key)`; `reminders_sent` по
  `(child_id, date)`; миграция v1→v2 (`SCHEMA_VERSION=2`) с бэкапом `<db>.v1.bak`
  и сидингом семьи №1 (конфиг авторитетен, legacy — только отсутствующие ID);
  tenant-aware data layer (`family_id` последним параметром, дефолт
  `DEFAULT_FAMILY_ID=1`) с обратной совместимостью; тесты изоляции семей.
- Вне 2A: регистрация/инвайты (2B), выбор активного ребёнка (2C), изоляция в UI (2D).

#### ✅ Подпроект 2B — Регистрация, инвайты и роли из БД (ВЫПОЛНЕНО 2026-09-17)

- Spec: `docs/superpowers/specs/2026-09-17-sp3b-registration-design.md`
- Plan: `docs/superpowers/plans/2026-09-17-sp3b-registration.md`
- Сделано: таблица `invites` (`SCHEMA_VERSION=3`); атомарные `create_family_with_owner`
  и `join_by_invite`; самопропускная регистрация в `/start` (создать семью / войти по
  коду) и deep-link `/start <token>`; карточки детей и экраны «Участники»/«Дети»;
  роли в боте через `MemberMiddleware` + `_role(member, uid)` с fallback на `.env`;
  в Mini App роль и `family_id` из `members`. Уже зарегистрированный пользователь не
  перемещается между семьями по коду.
- Вне 2B: выбор активного ребёнка (2C), роль doctor (Фаза 4).

#### ✅ Подпроект 2C — Активный ребёнок и tenant-aware данные (ВЫПОЛНЕНО 2026-09-17)

- Spec: `docs/superpowers/specs/2026-09-17-sp3c-active-child-design.md`
- Plan: `docs/superpowers/plans/2026-09-17-sp3c-active-child.md`
- Сделано: схема v4 (`members.active_child_id`, идемпотентная миграция); доступоры
  `set_active_child`/`resolve_active_child`/`count_family_children`; тенант-хелперы
  `_ctx`/`_child_name`/`_family_parents`; все data-пути бота и Mini App scoped по
  `(family_id, active_child_id)`; выбор ребёнка в боте (кнопка «🧒 Ребёнок» при >1)
  и в Mini App (`/api/children`, `PUT /api/active-child`, селектор в шапке);
  уведомления — родителям семьи; `backup_family_db` (family-scoped); временный gate
  2B снят (обслуживаются все семьи).
- Вне 2C: аналитика по всем детям сразу, роль doctor, мульти-семейный обход
  планировщиком, PostgreSQL (2D/Фазы 3–5).

#### ✅ Подпроект 2D — Мульти-семейный планировщик, dry-run, добивка изоляции (ВЫПОЛНЕНО 2026-09-17)

- Spec: `docs/superpowers/specs/2026-09-17-sp3d-finalize-design.md`
- Plan: `docs/superpowers/specs/2026-09-17-todo-plan.md` (этот документ)
- Сделано: планировщик обходит **все** семьи (`list_families` +
  `list_family_children`; семья №1 — fallback на `.env`-ребёнка), per-family часы
  напоминаний; недельный отчёт — **на каждого ребёнка**, родителям его семьи
  (дедуп `(child_id, "weekly")`); `get_measurement_by_id(..., child_id)` —
  defense-in-depth изоляция; автор записи в истории/CSV — из `members`, `.env`
  только при `member is None`; сквозные тесты изоляции chart/export Mini App;
  `scripts/migration_dry_run.py` — dry-run миграции без записи в источник
  (файловый снимок main + `-wal`, WAL-safe, источник не открывается SQLite).
- Вне 2D: PostgreSQL/Docker/REST (Фаза 3), PDF/геймификация (Фаза 4),
  PWA/графики (Фаза 5).

- [ ] **2.1** Таблица `families` (id, название, settings JSONB, подписка, created_at) — *частично 2A: `families(id, name, created_at)`*
- [ ] **2.2** Таблица `users` (id, family_id, role: parent/child/doctor, telegram_id, имя) — замена жёстких `CHILD_ID`/`PARENT_IDS` из `.env` — *2A: `members`; роли из БД ✅ 2B*
- [ ] **2.3** Таблица `children` (id, family_id, имя, дата рождения, целевая ПСВ) — поддержка мультидетных семей
- [ ] **2.4** `measurements.child_id` вместо `user_id`; миграция данных текущей семьи в схему v2 (версионированная миграция `SCHEMA_VERSION=2`) — *✅ 2A*
- [ ] **2.5** Бот: команда `/start` → регистрация/выбор семьи; deep-link приглашения (`/link <token>`) — *✅ 2B (`/start <token>`)*
- [ ] **2.6** Mini App: селектор ребёнка (если детей несколько), роль из БД вместо env — *✅ 2B (роль) + 2C (селектор: `/api/children`, `PUT /api/active-child`, `<select>` в шапке)*
- [ ] **2.7** Все проверки `is_parent`/`is_child` → проверка роли из БД (единый middleware/Depends) — *✅ 2B*
- [x] **2.8** Все хелперы `report.py` принимают контекст семьи (target, child_name) вместо глобальных констант — *✅ 2D (report.py чистый, контекст передаётся из bot/web)*
- [x] **2.9** Миграция: скрипт конвертации текущей prod-БД в новую схему с бэкапом и dry-run режимом — *базовая миграция ✅ 2A; dry-run ✅ 2D (`scripts/migration_dry_run.py`)*
- [x] **2.10** Тесты: изоляция семей (семья A не видит данные семьи B) — критично для публичного сервиса — *базовые изоляционные тесты ✅ 2A; сквозные UI-изоляции chart/export ✅ 2D*

### 🧊 Фаза 3 — Инфраструктура (ПО ТРЕБОВАНИЮ, без даты)

> Не планируется заранее. Берётся в работу, когда срабатывает соответствующий триггер.

| Пункт | Триггер |
|-------|---------|
| 3.1–3.3, 3.7 PostgreSQL + Alembic + asyncpg + PG-тесты | десятки семей с конкурентной записью / мультиворкеры / БД не влезает в файл |
| 3.4–3.5 Docker/compose/secrets | нужен переносимый деплой (`manage.sh`+systemd перестал устраивать) |
| 3.6 REST API v1 + JWT | нативное мобильное приложение / внешние интеграции (Mini App уже покрывает всё) |
| 3.8 очередь уведомлений | зафиксированы потери доставки |
| 3.8 разделение процессов (бот/веб/планировщик — сейчас один event loop) | сбои одного компонента стали влиять на другой / нагрузка |

### 🟢 Фаза 4 — Польза для пользователей (следующая)

- [x] **4.1** PDF-отчёты врачу (график+таблица+статистика; неделя/месяц/квартал; из бота и Mini App) — *✅ SP4A (`report_pdf.py`, кнопка «📄 Отчёт врачу», `GET /api/report/pdf`)*
- [x] **4.2** Геймификация (streak дни подряд, достижения «7/30/100 дней», бейджи) — *✅ SP4B (`gamification.py`, схема v5 `achievements`, экран «🏅 Достижения», `GET /api/gamification`, streak в статусе и Mini App)*
- [ ] **4.3** Доступ врача (read-only по ссылке + комментарии к замерам)
- [ ] **4.4** Метрики/structured logs (+ uptime; Prometheus/Grafana опционально)

> Лендинг/кабинет — удалить (решение владельца 2026-09-17: монетизация и публичный запуск исключены)

### 🟤 Фаза 5 — Развитие Mini App (параллельно с 4)

- [ ] **5.1** График: zoom/pan, тултипы (значение+дата+заметка), виды линия/столбцы/точки
- [ ] **5.2** Сравнение периодов (эта неделя vs прошлая) на одном графике
- [ ] **5.3** PWA: offline-кэш (IndexedDB), установка на домашний экран
- [ ] **5.4** Новые графики: распределение по зонам (pie), heatmap недели, прогноз тренда (линейная регрессия)
- [ ] **5.5** Корреляция заметок с падениями ПСВ («болел» → динамика)
- [ ] **5.6** UX-мелочи из аудита: busy-guard на note-save, кэш месяцев в shiftMonth, подсказка «переоткройте приложение» при истёкшем initData

---

## 3. Метрики успеха

| Метрика | Сейчас | Фаза 4 | Фаза 5 |
|---------|--------|--------|--------|
| Тесты | 519 ✅ | 470+ | 500+ |
| Блокирующих вызовов в async | 0 ✅ | 0 | 0 |
| Мульти-тенант (несколько семей) | ✅ (2A–2D) | ✅ | ✅ |
| Изоляция семей (тесты) | ✅ | ✅ | ✅ |
| Время ответа API | ~200 мс | <150 мс | <100 мс |

## 4. Вехи

| Веха | Дата | Критерий готовности |
|------|------|---------------------|
| **M0–M2** | ✅ 2026-09-17 | Фазы 0–2 (баги, async, мульти-тенант) |
| **M3: PDF врачу** | 2026-09-30 | SP4A: PDF из бота и Mini App |
| **M4: Функции** | 2026-10-31 | Геймификация + доступ врача + логи |
| **M5: Mini App** | 2026-11-30 | Фаза 5 |
| **Инфраструктура (б. Ф3)** | по требованию | триггеры из таблицы Фазы 3 |

## 5. Что уже хорошо (сохранить)

- ✅ 519 тестов, CI (pyflakes + compileall + pytest 3.11/3.12)
- ✅ HMAC-авторизация Mini App без пропусков
- ✅ Версионированные миграции (`PRAGMA user_version`)
- ✅ `report.py` — чистые общие хелперы; `escape_md`, csv-модуль
- ✅ WAL, параметризованный SQL, индекс `idx_meas_user_time`
- ✅ `manage.sh`: install/update/backup/restore/doctor/caddy, мульти-инстанс

---

## 6. Правила выполнения

1. Каждая фаза — через spec + plan (brainstorming → writing-plans) для нетривиальных задач
2. Каждый баг из аудита — с регрессионным тестом
3. Не трогать Фазу 2, пока не закрыта Фаза 0 (race-баги умножатся на количестве семей)
4. `roadmap.md` — исторический документ, актуальные статусы вести здесь